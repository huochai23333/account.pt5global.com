import {
  generateWorkspaceSystemHealthMetadata,
  renderWorkspaceSystemHealthPage,
} from "../workspace-global-pages";

export { generateWorkspaceSystemHealthMetadata as generateMetadata };

export default renderWorkspaceSystemHealthPage;
